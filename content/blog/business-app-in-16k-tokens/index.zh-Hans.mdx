---
title: "一个业务应用到底有多少 token？完整 CRM 全应用不到 150k"
description: "完整 CRM 的数据模型、流程、权限与界面合计不到 150k token：业务逻辑不到 100k，UI 约 50k；随包参考 CRM 仍约 16k。能整体装进智能体上下文的软件，维护方式完全不同。"
author: ObjectStack Team
date: 2026-07-22
status: published
topic: ai-agents
audience: developer
solutions:
  - crm
industries: []
cover: ./cover.svg
tags:
  - AI 智能体
---

**一句话版本：** 一套完整 CRM 可以压进不到 **150,000 token** 的类型化元数据：业务逻辑——全部对象、流程、动作和权限——不到 **100,000 token**，声明式 UI 约增加 **50,000 token**。随包的 `app-crm` 参考样例更小：31 个文件、1792 行，约 **16,000 token**。这个 16k 描述的是参考样例，不是产品级完整 CRM 的上限。两者都装得进 200k 上下文窗口。我们称之为**上下文体量的软件（context-sized software）**：小到 AI 智能体能整体装入、推理和重构，而人只需审阅变更 diff。

## 自己算一遍

16k 不是完整产品的基准，而是开源 [ObjectStack 框架](https://github.com/objectstack-ai/objectstack)随包 CRM 参考样例的可复现计数：

```bash
git clone https://github.com/objectstack-ai/objectstack
find objectstack/examples/app-crm/src -name '*.ts' -not -name '*.test.ts' \
  | xargs cat | wc -l
# → 31 个文件共 1792 行 ≈ 16k token（按约 4 字符/token 估算）
```

| 系统 | 文件数 | 行数 | ≈ Token | 能装进 200k 上下文？ |
|---|---:|---:|---:|---|
| CRM 示例（[`app-crm`](https://github.com/objectstack-ai/objectstack/tree/main/examples/app-crm)）——对象、视图、仪表盘、流程、权限、翻译 | 31 | 1792 | ~16k | 能——还有 12 倍余量 |
| [HotCRM](https://github.com/objectstack-ai/hotcrm)——完整的市场级 CRM：15 个对象、17 个流程、4 个仪表盘、2 个 AI copilot、4 种语言 | 132 | ~18,000 | 总计不到 150k（业务逻辑不到 100k + UI 约 50k） | 能——余量超过 50k |
| 传统手写 CRM 代码库 | 数千 | 30 万–100 万+ | 数百万 | 差得远 |

同一个产品品类，两种有用尺度：约 16k 的参考样例，以及不到 150k 的完整 CRM。两者都达到上下文体量；传统代码库没有。

## 为什么 token 成了正确的度量单位

五十年来我们用代码行数度量软件，因为约束条件是人类读者。现在读者换了。AI 智能体要*安全地*修改一个系统，得先把系统装进上下文——这把所有代码库分成两种状态：

- **系统大于上下文。** 智能体只能 grep、抽样、猜测。它的修改是局部的，错误却是全局的。每次变更都是隔着钥匙孔做考古。
- **系统小于上下文。** 智能体一次读完所有东西——每个对象、每条权限规则、每个依赖。*「改这里会弄坏什么？」*从一个愿望变成一个可回答的问题。跨领域的变更——把一个概念在数据模型、权限、API 和界面里统一重命名——是一个连贯的 diff。

这不是渐进式改善，是状态切换。而分界线就画在你的上下文窗口所在的位置。

## 业务逻辑为什么能保持在 100k token 以内

不靠压缩技巧，靠删除。一个企业应用是两样东西交织在一起：

- **决策。** 有哪些对象、它们如何关联、谁能看到哪个字段、线索转化时发生什么。这是你的业务**本体（ontology）**，真正不可约减——只有你能做这些决定。
- **管道。** 数据表、CRUD 接口、列表页和详情页、权限中间件、审计写入。这些在古往今来每一个业务应用里几乎一模一样——也就意味着它们是*可派生的*。

ObjectStack 押的注是：制品里只保留决策，管道全部由运行时派生。在随包参考 CRM 里，那 1792 行*就是*决策清单——类型化、经 Zod 校验的元数据。数据库 schema、REST API、渲染后的界面和 MCP 工具在每次启动时由它计算出来。可派生的东西一概不存储，所以不会漂移。到了完整 HotCRM，这种分离仍把业务逻辑控制在 100k token 以内；声明式 UI 约增加 50k，使全应用保持在 150k 以内。

## 什么压缩不了

对边界诚实，比推销更重要：

- **新颖算法。** 定价优化器、自定义撮合引擎，过去是真代码，现在还是真代码。格式留有逃生舱——action 和插件就是普通 TypeScript。
- **像素级定制体验。** Schema 驱动的 UI 渲染企业软件的模式非常好；但品牌官网和游戏不是这么做的。
- **奇特的集成。** 对接一个三十年前的 SOAP 服务，连接器代码还是连接器代码。

所以这个论断比「一切皆元数据」更窄——也因此更强：构成企业软件主体的 CRUD + 工作流内核是可派生的，而真正重要的残差——本体——装得进上下文。

## 软件达到上下文体量后，什么变了

1. **评审变成真的。** 1792 行是一次代码评审；30 万行是一场仪式。人能读完整个 diff，校验门和运行时的权限、审计强制在后面兜底。
2. **维护不再腐烂。** 整个系统装得进上下文时，智能体是整体重构，而不是修补它看得见的那部分。企业软件常见的熵增曲线——每次变更都比上一次更危险——被拉平了。
3. **本体始终归你。** 定义是 Apache-2.0 的开放格式，是你仓库里的普通文件——人能读，任何智能体能写，可跨运行时迁移。

## 跑一遍这个循环

把你的编码智能体指向 ObjectStack——脚手架自带 `AGENTS.md` 和技能包，智能体一开始就加载了这个格式的规则：

```bash
npm create objectstack@latest my-app && cd my-app
npx os dev --ui   # 应用已运行——把下一个需求直接告诉你的智能体
```

更想在浏览器里完成？[ObjectOS](https://www.objectos.ai) 是同一个思路的托管形态——在线构建与问询，内置 AI Builder、权限与审计。
