---
title: EU AI Act 八月落地、CADA 提案：你的 AI 运行时，准备好被审计了吗
description: 审计员坐下来，只问了一个问题——把 2 月 14 日那笔折扣的完整经过调出来。然后房间安静了。EU AI Act 八月强制执行、CADA 提出主权框架，但 78% 的高管没信心 90 天内通过审计。问题几乎从不在模型，而在运行时——而可审计性，是没法在最后一个季度临时补上的。
author: ObjectStack Team
date: 2026-06-16T15:00:00+08:00
status: published
topic: governance
audience: it
solutions: []
industries: []
cover: ./cover.svg
tags:
  - EU AI Act
  - CADA
  - 合规
  - 数据主权
  - 自托管
  - AI 治理
---

设想一个会议室。

审计员坐下，打开电脑，没有寒暄，问了第一个问题：

> "把 2 月 14 日那笔超额折扣的完整经过调出来给我看——谁发起的、AI 在里面做了什么、依据哪条权限、谁批准的。"

然后房间安静了。

不是因为这笔折扣有问题，而是因为这笔折扣里 AI 参与的那一段，**在系统里没有一个完整、连续、不可抵赖的地方能调出来**。客户数据来自一个系统，折扣动作落在另一个系统，AI 那次调用的记录（如果记了的话）又在第三个地方，而且和人的操作不在一本账上。要拼出审计员要的那条线，得三个团队翻三天日志，还不保证拼得齐。

这个安静的会议室，就是 2026 年很多企业即将面对的场景。

## 倒计时是真的，而且很近

EU AI Act 在 2026 年 8 月开始强制执行；就在这个月初，6 月 3 日，欧盟又提出了《云与人工智能发展法案》（CADA），第一次给云和 AI 服务定了全欧统一的四级主权框架。对在欧洲有业务、或服务欧洲客户的企业，这是几周之内的事。

而最扎心的数字是：调研显示 **78% 的高管没有信心，能在 90 天内通过一次独立的 AI 治理审计**。不是不想合规，是真到那个会议室里，答不上来。

## 审计查的不是模型，是运行时

很多团队为合规做的第一件事是评估模型——有没有认证、会不会输出有害内容。这些重要，但它们不是审计的主战场。监管真正要的，是你能否为**每一次 AI 触碰业务的动作**说清三件事：

| 审计员会问 | 它在查什么 | 答不上来的后果 |
| --- | --- | --- |
| 这些数据处理得合法吗？ | 数据在哪、谁能读、有没有出过欧盟 | 数据驻留违规，CLOUD Act 暴露 |
| 这个动作谁授权 AI 做的？ | 改折扣、发合同按谁的权限算 | 责任无法归属，动作不可控 |
| AI 参与的那步留痕了吗？ | 能否拿出完整、不可抵赖的证据链 | 审计直接失败 |

没有一个问题是关于模型的。它们全都关于模型之外那一层——承载数据、强制权限、记录证据的运行时。

## 可审计性，是补不上的

这里有个很多人没意识到的硬道理：**合规不是一份你在 Q3 赶出来的文档，而是一个系统要么具备、要么不具备的属性。**

那条审计员想要的证据链，必须在动作**发生的那一刻**就被记下来。如果你的运行时当时没记，8 月之前再怎么突击补救，都变不出 2 月 14 日那一刻的真实经过——你只能"重建"，而重建出来的记录，审计员一眼就知道不能采信。

这就是为什么"先上 AI、合规以后再说"在 2026 年是个危险的赌注：你赌的是审计不会问到那些当时没记的动作。可一旦问到，那段时间的所有 AI 动作就成了一片无法举证的空白。

## 把规则写进提示词，为什么过不了这一关

不少团队其实做了治理，只是做错了地方——写进了提示词。"你只能查当前用户有权限的数据""折扣超过 30% 要提示审批"，平时看着也管用。但它过不了审计，两条原因都致命：

第一，**提示词是建议，不是控制。** 它靠模型"愿意听话"生效，一次越狱、一个没覆盖的边界就被绕过。审计员问"你怎么*保证*销售看不到别人的提成"，"我们在提示词里写了请不要"不是能签字的答案。

第二，**提示词不产生记录。** 审计要的是结构化、不可抵赖的证据，提示词给不了。

合规真正要求的，是把治理从"提示词里的建议"挪到"执行引擎里的强制"——权限在每次读写时被运行时核验，审计是动作的天然副产品。

## CADA 之后，"数据在哪"成了硬指标——但自托管不是免罪符

CADA 把另一件事推到台前：数据主权。它本质在问，你的数据和 AI 处理过程，到底在谁的管辖权之下？这一问，直接指向部署形态。把读取真实业务数据的 AI 运行时，交给一个你无法审视内部、又受 CLOUD Act 管辖的外部 SaaS，在新框架下越来越难签字。自托管能一次性改善数据驻留、第三方暴露和可见性——要自托管的不一定是模型，而是承载对象、权限、工具、审批和审计证据的那个运行时。

但要诚实地说一句：**自托管不是一张免罪符，它是一笔交易。** 当运行时跑在你自己的基础设施上，打补丁、轮换密钥、保证日志不丢、出事时一键熔断——这些操作和举证责任，也一起落到了你头上。你换来的是控制权，代价是责任。反过来，如果你希望让别人替你担这份责任，那通常意味着把数据也交给他。这笔账没有白拿的一边，得看哪边的代价你担得起、签得了字。

## 一个能被签字的运行时，长什么样

把上面的要求倒过来，就是验收清单。最直接的检验，不是看产品介绍，是看它能不能当场吐出审计员要的那条记录。在 ObjectOS 上，开头那笔折扣调出来是这样一条记录：

```json
{
  "event": "opportunity.discount.update",
  "actor": { "type": "agent", "on_behalf_of": "user:zhang.san" },
  "object": "crm_opportunity/OP-2291",
  "change": { "discount_percent": { "from": 15, "to": 35 } },
  "permission_checked": "crm_sales_user → allowEdit: true",
  "approval": { "rule": "discount>30%", "approved_by": "user:li.si", "at": "2026-02-14T10:22:11+08:00" },
  "timestamp": "2026-02-14T10:19:03+08:00"
}
```

这条记录里，谁、做了什么、依据哪条权限、谁批的、何时——全在。它不是事后补的工程，是 agent 以张三的身份调用受治理工具时，运行时顺手落下的一笔。人和 agent 走同一套权限引擎、记同一本账。业务定义（对象、权限、流程）则是你仓库里可 diff、可追溯的元数据——审计员问"销售能读哪些数据"，答案不在某人脑子里，在权限集里写着。

## 结语

距 8 月只剩几周。回到那个会议室：如果审计员现在就坐下，问你要 2 月某笔操作里 AI 那一段的完整经过，你的系统能不能在那一刻、不靠三个团队翻三天日志，把它吐出来？

答不上来，几乎从不是因为模型不行，而是因为治理被放错了层——放进了提示词，而不是运行时。把它挪回运行时，那条证据链才会在你需要之前就已经在那里。这件事补不上，只能现在就让它成立。

```bash
npm i -g @objectstack/cli && os start
```

让一个 agent 以某个用户的身份去改一条数据，然后翻开审计记录——你会看到审计员想要的那条线，本来就在。
